import {View, StyleSheet} from 'react-native';
import React from 'react';

const BottomHeight = () => {
  return <View style={styles.bottomwidth} />;
};

export default BottomHeight;

const styles = StyleSheet.create({
  bottomwidth: {
    height: 50,
  },
});
